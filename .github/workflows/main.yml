name: CI/CD Pipeline with Docker

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm install

      - name: Run unit tests
        working-directory: ./backend
        run: npm test

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push backend Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/wellmart-backend:latest

      - name: Build and push frontend Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/wellmart-frontend:latest

      - name: Simulate Deploy to EC2 (manual due to security rules)
        run: |
          echo "🚫 Deployment ke EC2 tidak dilakukan secara otomatis karena:"
          echo "- Soal UTS mewajibkan Security Group SSH hanya dari IP publik pribadi."
          echo "- GitHub Actions berjalan dari IP publik GitHub, bukan IP pribadi saya."
          echo "✅ Oleh karena itu, proses deployment dilakukan manual melalui SSH dari laptop saya."
          echo "Langkah-langkah manual termasuk: docker pull, docker stop/rm, docker run frontend & backend, serta restart nginx."
