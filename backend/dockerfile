# Gunakan image resmi Node.js sebagai image dasar
FROM node:18-alpine

# Tetapkan direktori kerja di dalam container
WORKDIR /app

# Salin file package.json dan package-lock.json
COPY package*.json ./

# Unduh dan instal dependensi proyek
RUN npm install

# Salin seluruh kode sumber aplikasi
COPY . .

# Paparkan port yang akan digunakan aplikasi (port backend)
EXPOSE 3000

# Perintah default yang akan dijalankan saat container dimulai
# Pastikan ini menunjuk ke app.js
CMD ["node", "app.js"]